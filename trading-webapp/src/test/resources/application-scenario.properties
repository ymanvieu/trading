spring.jpa.generate-ddl=true
spring.flyway.enabled=false
spring.jms.listener.auto-startup=false
trading.scheduler.type=none

provider.yahoo.url.history=https://yahoo-history/{symbol}?range={range}
provider.yahoo.url.lookup=https://yahoo-lookup/searchassist;searchTerm={symbol}
